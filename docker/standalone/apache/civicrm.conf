# CiviCRM-specific apache configuration; the virtual host lives in
# sites-available/000-default.conf, civikitchen's own extras in civikitchen.conf.

# Silences the complaint from Apache that the ServerName has not been set.
ServerName localhost
