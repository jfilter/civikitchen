# Static analysis of the test suite — part of the standard CI contract.
#
# The main phpstan.neon.dist analyses production code only. This second pass
# keeps test helpers and assertions at the same level instead of allowing a
# weakly typed test layer to hide defects in otherwise strict production code.
#
# Do not add a baseline. Existing findings are work to fix, not a reason to
# remove this configuration or make the gate optional.
#
# What it buys beyond level 10 on test code: the test-only rules in
# civikitchen/phpstan-ck-legacy only ever see a file that is analysed, and
# ck.test.customFieldInTransaction / ck.test.extensionInTransaction /
# ck.test.ddlInTransaction are inert everywhere until then — DDL inside a
# TransactionalInterface test is the flake whose own file stays green.
includes:
	- phpstan.neon.dist

parameters:
	# Level 10 here too. Test code that cannot carry the same level is test
	# code that may not catch the bug it was written for.
	level: 10
	# `!` overrides the included list instead of appending to it — this is a
	# second pass over the tests, not a re-run of the production analysis.
	paths!:
		- tests/phpunit
	# The second pass replaces production `paths`; scan extension classes so
	# APIv4 façades loaded by CiviCRM's runtime classloader remain known while
	# only tests themselves are analysed for findings.
	scanDirectories:
		- Civi
		- CRM
	# PHPUnit's own classes come from core's dev dependencies. A packaged
	# install may not have them; point phpstan at wherever they are, e.g.:
	#	scanDirectories:
	#		- /var/www/html/core/vendor/phpunit/phpunit/src
